{
  "name": "url-replace-2",
  "version": "0.0.0",
  "description": "Node proxy for redirecting selected http calls to different hosts.",
  "main": "proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-proxy": "^1.13.2"
  }
}
