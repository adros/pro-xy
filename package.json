{
  "name": "url-replace",
  "version": "0.0.2",
  "description": "Node proxy for redirecting selected http calls to different hosts.",
  "main": "proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "bin": {
    "url-replace": "./bin/url-replace.js"
  },
  "repository": "https://github.com/zboro/url-replace.git",
  "author": "zboron.lukas@gmail.com",
  "dependencies": {
    "http-proxy": "^1.13.2"
  }
}
